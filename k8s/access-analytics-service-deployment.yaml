apiVersion: apps/v1
kind: Deployment
metadata:
  name: access-analytics-service
  namespace: cse474-prod
  labels:
    app: access-analytics-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: access-analytics-service
  template:
    metadata:
      labels:
        app: access-analytics-service
    spec:
      containers:
        - name: access-analytics-service
          image: your-dockerhub/access-analytics-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3007
          envFrom:
            - configMapRef:
                name: access-analytics-service-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 3007
            initialDelaySeconds: 10
            periodSeconds: 10
