apiVersion: apps/v1
kind: Deployment
metadata:
  name: backup-service
  namespace: cse474-prod
  labels:
    app: backup-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backup-service
  template:
    metadata:
      labels:
        app: backup-service
    spec:
      containers:
        - name: backup-service
          image: your-dockerhub/backup-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3008
          envFrom:
            - configMapRef:
                name: backup-service-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 3008
            initialDelaySeconds: 10
            periodSeconds: 10
