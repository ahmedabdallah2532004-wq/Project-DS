{
  "title": "PM3 Microservices Dashboard",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "panels": [
    {
      "type": "timeseries",
      "title": "metrics-service HTTP Requests",
      "targets": [
        {
          "expr": "sum(rate(metrics_service_http_requests_total[5m])) by (status_code)",
          "legendFormat": "metrics-service {{status_code}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 0
      }
    },
    {
      "type": "timeseries",
      "title": "notification-service HTTP Requests",
      "targets": [
        {
          "expr": "sum(rate(notification_service_http_requests_total[5m])) by (status_code)",
          "legendFormat": "notification-service {{status_code}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 0
      }
    },
    {
      "type": "timeseries",
      "title": "preview-service HTTP Requests",
      "targets": [
        {
          "expr": "sum(rate(preview_service_http_requests_total[5m])) by (status_code)",
          "legendFormat": "preview-service {{status_code}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 8
      }
    },
    {
      "type": "timeseries",
      "title": "compression-service HTTP Requests",
      "targets": [
        {
          "expr": "sum(rate(compression_service_http_requests_total[5m])) by (status_code)",
          "legendFormat": "compression-service {{status_code}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 8
      }
    },
    {
      "type": "timeseries",
      "title": "webhook-service HTTP Requests",
      "targets": [
        {
          "expr": "sum(rate(webhook_service_http_requests_total[5m])) by (status_code)",
          "legendFormat": "webhook-service {{status_code}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 16
      }
    },
    {
      "type": "timeseries",
      "title": "scheduler-service HTTP Requests",
      "targets": [
        {
          "expr": "sum(rate(scheduler_service_http_requests_total[5m])) by (status_code)",
          "legendFormat": "scheduler-service {{status_code}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 16
      }
    },
    {
      "type": "timeseries",
      "title": "access-analytics-service HTTP Requests",
      "targets": [
        {
          "expr": "sum(rate(access_analytics_service_http_requests_total[5m])) by (status_code)",
          "legendFormat": "access-analytics-service {{status_code}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 24
      }
    },
    {
      "type": "timeseries",
      "title": "backup-service HTTP Requests",
      "targets": [
        {
          "expr": "sum(rate(backup_service_http_requests_total[5m])) by (status_code)",
          "legendFormat": "backup-service {{status_code}}"
        }
      ],
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 24
      }
    }
  ]
}
