apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.audit.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.audit.service.type }}
  ports:
  - port: {{ .Values.audit.service.port }}
    targetPort: {{ .Values.audit.service.port }}
    protocol: TCP
  selector:
    app: {{ .Values.audit.name }}
