{{- range $name, $svc := .Values.services }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $name }}-config
  namespace: {{ $.Values.namespace }}
data:
{{- range $key, $value := $svc.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
